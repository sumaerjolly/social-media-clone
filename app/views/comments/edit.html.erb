<%= form_for [@post, @comment], :html => {class: "form-horizontal", role: "form"} do |f| %>
    <% if @comment.errors.any? %>
        <div class="panel panel-danger col-md-offset-1">
            <div class="panel-heading"> 
                <h2 class="panel-title"><%= pluralize(@comment.errors.count, "error") %> prohibited this article from being saved: </h2>
                <div class="panel-body"> 
                    <ul>
                        <% @comment.errors.full_messages.each do |msg| %> 
                            <li>
                                <%= msg %> 
                            </li>
                        <% end %> 
                    </ul>
                </div> 
            </div>
        </div> 
    <% end %>
    <div class="container">
    <h3 align = "center">Edit Comment </h3>
        <div class="form-group row">
            <div  class="col-sm-2 control-label">
                
            </div>
            <div class="col-sm-8 text-center">
                <%= f.text_area :body, class: "form-control",  autofocus: true %>
            </div>
        </div> 
    <div class="form-group row">
        <div  class="col-sm-5 control-label">  
        </div>      
        <div class="col-sm-5 col-sm-offset-2 ">
            <%= f.submit class: 'btn btn-primary btn-lg' %>
        </div>
    </div>
    </div>
   
<% end %>






